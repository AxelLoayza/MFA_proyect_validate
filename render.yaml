services:
  - type: web
    name: api-container
    env: python
    rootDir: apiContainer
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python src/main.py
    autoDeploy: true